#If minION output was not base called insitu run guppy base caller to get nucleotide sequences.
#my exact command guppy_basecaller --compress_fastq -i fast5/ -s ../7_5_22/ -c dna_r9.4.1_450bps_hac.cfg

guppy_basecaller --compress_fastq -i [input directory] -s [new output directory] -c [configeration file]

#De multiplexing with Porechop
#My exact command from within the dir containing the basecalled reads
# for file in *.gz; do porechop -i ${file} -b binned; done

for file in *.gz do porechop -i [input file(s)] -b [output directory]; done

#Canu genome assembler perl script
#Due to not being able to add canu perl script to PATH I created a persistant enviromental variable to path where canu is installed
#CANUSCRIPT=/home/aec77/canu-2.2/bin/canu  added to .bashrc 

perl $CANUSCRIPT/canu -p 3795c3014_assembly1 genomeSize=1m stopOnLowCoverage=0.1 -nanopore BC01.fastq

https://github.com/fenderglass/Flye
